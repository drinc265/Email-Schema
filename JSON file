{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "email": {
      "type": "string",
      "format": "email",
      "description": "The email address of the credential holder"
    },
    "issuer": {
      "type": "string",
      "description": "The entity that issued the credential"
    },
    "issuanceDate": {
      "type": "string",
      "format": "date-time",
      "description": "The date and time when the credential was issued"
    },
    "expirationDate": {
      "type": "string",
      "format": "date-time",
      "description": "The date and time when the credential expires (optional)"
    }
  },
  "required": [
    "email",
    "issuer",
    "issuanceDate"
  ]
}
